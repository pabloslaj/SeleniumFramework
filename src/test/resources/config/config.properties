# App environment: qa, dev, preprod
environment=dev

#Runmode: local - remote
runMode=local

#RemoteMode: SELENIUM
remoteMode=selenium

#Selenium Grid URL
seleniumGridUrl=http://localhost:4444/wd/hub

# RetryFailedTests
retryFailedTests=false
